/**
 * userService.js — Firestore User Profile Service
 *
 * Operations:
 * - getUser(uid) → Fetch user profile
 * - updateStats(uid, result) → Update win/loss/draw counters
 * - calculateRating(current, opponent, result) → ELO formula
 *
 * @module services/userService
 */

// TODO: Implement in Phase 4
